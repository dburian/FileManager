namespace FileManager
{
	/// <summary>
	/// More descriptive and user frienly type of job than JobType
	/// </summary>
	public enum JobTypeDescription
	{
		FileMove,
		FileCopy,
		DirMove,
		DirCopy,
		DirDelete,
		FileDelete
	}
}
